# ArmDemo Engineering Log — 2026.8.9

## English

### Goals

- Configure the driver controls to use a PS5 controller on USB port 0.
- Add the initial voltage and angle-limit constants for the arm.

### Completed Work

- Replaced `CommandXboxController` with `CommandPS5Controller` in `RobotContainer` while retaining driver controller port 0.
- Updated the controller-related documentation to reference the PS5 controller.
- Added arm voltage, minimum-angle, and maximum-angle constants.
- Verified that the project builds successfully with the WPILib 2026 Java environment.

## 中文

### 今日目标

- 将驾驶员控制手柄配置为 USB 端口 0 上的 PS5 手柄。
- 添加机械臂初始电压和角度限制常量。

### 已完成工作

- 在 `RobotContainer` 中将 `CommandXboxController` 替换为 `CommandPS5Controller`，并保持驾驶员手柄端口为 0。
- 更新手柄相关说明，使其与 PS5 手柄配置一致。
- 添加机械臂电压、最小角度和最大角度常量。
- 使用 WPILib 2026 自带的 Java 环境成功完成项目构建验证。
